<Page x:Class="OpenIddict.Sandbox.UnoClient.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:OpenIddict.Sandbox.UnoClient.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <utu:NavigationBar Content="{Binding Title}" />

    <StackPanel Grid.Row="1"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Spacing="16">
      <TextBox Text="{Binding Name, Mode=TwoWay}"
          PlaceholderText="Enter your name:" />

      <Button Name="LocalLogout" Content="Log out from the local server"
              Command="{x:Bind ViewModel.LogoutCommand}"
              HorizontalAlignment="Center"
              Height="64" Width="540" FontSize="20" />

      <Button Name="Cancel" Content="Cancel"
              Command="{x:Bind ViewModel.CancelCommand}"
              HorizontalAlignment="Center"
              Height="64" Width="540" FontSize="20" />

    </StackPanel>
  </Grid>
</Page>
